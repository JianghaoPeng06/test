# JasperPeng — 个人设计 / 艺术 / 世界观 / 研究档案站

> 这份 README 是给「重开对话后继续做这个项目」用的交接文档。
> 记录了架构决策、为什么这么做、踩过的坑、当前进度和待办。

---

## 1. 这是什么

一个纯静态个人档案网站，六个板块：Research / Works / Universe / Characters / About / Contact。

- **设计源**：`my web figma.svg`（19MB，已放进项目根目录，最终依据）
  早先那份 `Untitled.svg` 旧稿已不在项目里
- **视觉蓝本**：openai.com 为主，apple.com 为辅
- **技术栈**：无框架、无构建依赖的纯 HTML / CSS / JS。只有一个 PowerShell 脚本做静态生成
- **运行方式**：VS Code 的 **Live Server**（用户实际用的就是这个）；`file://` 直接打开也做了兼容

---

## 2. 文件结构

```
D:\Claude\figma\
├── index.html              扉页（全屏封面，无导航，点 Explore 进首页）
├── home.html               首页
├── article.html            文章模板    ?a=<slug>
├── category.html           分类模板（**生成源**，含 {{TOKEN}}，不要直接访问）
├── characters/index.html   角色页（深色锁定）  ?c=<slug>
│
├── js/data.js              ★ 全站唯一数据源（板块 / 分类 / 文章 / 角色）
├── js/assets.js            图片清单（build.ps1 自动生成，不要手改）
├── js/main.js              全部交互行为
├── css/style.css           设计系统 + 全部样式
│
├── build.ps1               ★ 静态生成器
├── 刷新图片.cmd            换完图双击它（就是跑一次 build.ps1）
├── 本地预览.ps1            简易静态服务器（5599），行为对齐 Live Server
│
├── assets/images/          47 张**真实照片**（46 个 slug + poster.jpg，共 5.4MB）
│                           **每一张都不一样**，取自 `网页图片/`，见 §3.4
│                           换图 = 用自己的图盖掉同名文件，什么都不用删
│   └── .placeholders.txt   占位图登记表（记 SHA256，build.ps1 维护，别手改）
│                           现在是**空的** —— 一张占位图都不剩了
├── assets/icons/favicon.svg
│
├── works/ research/ universe/ characters/ resources/   18 个生成的分类页
│                           （`characters/oc/` 与 `characters/character/` 不再生成
│                             —— 这两项都直接跳角色页）
│
├── 网页图片/                用户提供的 85 张原始素材（248MB）。
│                           §3.4 那张对照表就是从这里挑的；**部署时不要上传这个目录**
│
└── my web figma.svg        Figma 导出的设计稿（19MB，别删 ——
                            版式数值全从它量的；早先那批占位图也是从它里面取的）
```

---

## 3. 核心架构决策（以及为什么）

### 3.1 单一数据源 + 静态生成

`js/data.js` 是唯一数据源，定义了板块、分类、文章、角色。

`build.ps1` 读它，按这个顺序做四件事：

1. **占位图**（兜底，现在一次都不会触发）：某个 slug 若一张真图都没有，
   就从 `my web figma.svg` 里内嵌的照片现补一张 `<slug>.png`（见 §3.4）。
   已经有真图就跳过，绝不会把真图盖掉 —— 现在 46 个 slug 全有真图，这一步是空转。
2. **图片清单** `js/assets.js`：扫描 `assets/images/**`，记下每个 slug 实际存在的文件（见 §3.4）
3. **分类页**：从 `category.html` 模板生成 **18 个**，套
   `{{BASE}} {{DIR}} {{SECTION}} {{CAT}} {{TITLE}} {{DESC}}`；
   带 `to:` 的分类不生成，旧目录还会被删掉（见 §3.5）
4. **注入**：把页头 / 页脚写进每个页面 `<!-- #chrome:header:start -->` 与 `:end` 之间；
   顺手补上 `js/assets.js` 的 `<script>`（必须排在 `data.js` 之前），
   并按清单校正静态 HTML 里手写的图片路径

改了 `js/data.js` 或 `category.html` 之后重跑一次即可：

```bash
powershell -ExecutionPolicy Bypass -File build.ps1
```

> **为什么页头页脚是静态 HTML 而不是 JS 注入？**
> 早期版本用 JS 注入头尾，结果 JS 一失败页面就只剩一对空标签，
> 连导航都没有。改成静态注入后：没有 JS 也是可导航的完整站点，
> 而单一数据源靠构建脚本保住。

### 3.2 路径解析

每个页面 `<body data-base="...">`：根目录为空，`/works/artifacts/` 下为 `../../`。
JS 拼链接时一律加这个前缀，所以任何层级都指得准。

`file://` 下浏览器不会把目录地址解析成 `index.html`，
所以 `main.js` 的 `patchDirLinks()` 会在 `location.protocol === 'file:'` 时补全文件名。
部署到服务器则保持 `/works/artifacts/` 整洁地址。

### 3.3 栅格是「推导」的，不是坐标搬运

Figma 坐标不直接翻译成 CSS。优先级链条（来自用户给的提示词 §18，很关键）：

> **信息架构 > 视觉层级 > 比例关系 > 具体像素坐标**

容器 `--content: 1240px`，用 grid + gap 推导，在 1280 屏上恰好还原设计稿：

```
3 列大卡  (1240 - 44×2) / 3 = 384   ← 设计稿值
2 列列表  (1240 - 70)   / 2 = 585   ← 设计稿值
页脚 5 列 (1240 - 110×4)/ 5 = 160   ← 设计稿值
```

用户的栅格数值经核算是**精确的**，不要改容器宽度。

> ⚠ 例外：卡片间距 `--gap-card` 已从 Figma 的 44px 收到 **32px**（openai 实测约 24，取折中）。
> 这是用户明确要求「向 openai 靠拢」后的偏离，容器宽度 1240 保持不动。

### 3.4 图片路径 —— 每个 slug 一张真实照片，互不重复

`data.js` 里**不写扩展名**，只写 slug。

**现在 46 个 slug + 首页海报，一共 47 张图，全部是用户自己的照片，而且张张不同。**
换图 = **用自己的图盖掉同名文件**，不用删任何东西、不用改任何代码，
刷新一下就是新图（换成别的扩展名才需要跑一次构建，见下）。

> 为什么要「张张不同」：上一版 46 张封面全是同一批 Figma 照片的裁切版，
> 用户原话是「我自己想修改的时候无法分辨该改哪一张」。所以他在项目里放了
> `网页图片/`（85 张原始素材），现在每个 slug 都从里面挑了一张**语义对得上**的，
> 打开页面就知道哪张对应哪个 slug。**占位图这个概念现在已经没有实例了**
> —— `.placeholders.txt` 是空的，但那套机制留着（见下），万一新增 slug 还用得上。

**图源分配**（`网页图片/` → slug，挑的时候尽量对上内容）：

| slug | 素材 | 为什么 |
|---|---|---|
| `works/indian-anklets` | `IMG_1785.JPG` | 手里那串编织脚铃，正是文章讲的东西 |
| `works/tongtong-mask` | `IMG_5276.JPG` | 做好的白面具 |
| `characters/tong` | `IMG_5239.JPG` | 同一张面具的制作中途 —— 嗵嗵这个角色就是从面具反推出来的 |
| `works/osaka` | `IMG_6986.JPG` | 道顿堀霓虹 |
| `works/kyoto` | `IMG_7229.JPG` | 伏见稻荷千本鸟居 |
| `universe/noote-metro` | `SHUNIU (1).PNG` | 一张真的线网枢纽图 |
| `works/doudou-seasons` | `DOUDOU1.jpg` | DOUDOU 春夏秋冬 TOURLIVE 标识 |
| `characters/caomeijiang` `la` `qi` | `草莓酱/拉/祈设定3视图` | 角色本人的三视图 |
| `poster.jpg` | `IMG_9138.JPG` | 黄昏旋转木马，节庆海报的调子 |
| 其余 | 见 `build.ps1` 旁边那份对照 | 按题材配（铁道 / 河景 / 樱花 / 雏人形 / 光晕…） |

尺寸与裁法：

| 用途 | 尺寸 | 裁法 | 存成 |
|---|---|---|---|
| 文章封面（卡片全是 1:1） | 800×800 | 居中裁切 | `.jpg`（质量 84） |
| 7 个角色立绘 | 长边缩到 900×1200 之内 | **不裁、不留边**，保留原比例 | `.jpg`（源是 png 的存 `.png`） |
| `poster.jpg`（首页 16:9 海报） | 1600×900 | 居中裁切 | `.jpg` |

> 立绘为什么不留边：`.chars__art` 是 `object-fit: contain`，页面自己会留边。
> 生成时再补一层白底，深色的角色页上就会多出一块白方块。
>
> 为什么整批换成 JPEG：上一版 47 张 PNG 占 14.1MB，首页一屏就要拉两三兆，
> 也是「加载新页面很卡」的一部分。同样尺寸的 JPEG 只有 5.4MB，肉眼看不出差别。
>
> 转换脚本一次性用完就没留在项目里（在会话临时目录）。要重做的话，
> 关键点只有三条：EXIF 方向（`PropertyItem 274`）必须先转正，否则手机照片会躺着；
> `System.Drawing` 存 JPEG 要显式给 `EncoderParameter(Quality)`；
> `Image::FromStream` 之后那个 `MemoryStream` 不能关。

#### 占位图机制（现在没有实例，但机制还在）

`build.ps1` 只在「某个 slug 一张图都没有」时才生成占位图，并把 SHA256 记进
`assets/images/.placeholders.txt`：

- 哈希**还对得上** → 还是占位图，在清单里排到最后，任何真图都压得过它
- 哈希**对不上** → 用户已经把这张图换掉了，当真图处理

于是两种换图方式都成立：

1. **直接盖掉同名文件** —— 最省事，刷新页面即可
2. **另丢一张别的扩展名** —— 占位图会自动让位，跑一次构建即可

真实文件名由两层决定：

1. **构建期**：`build.ps1` 扫 `assets/images/**` 生成 `js/assets.js`
   （`window.JP_ASSETS`，键是 `<目录>/<slug>`）。同一 slug 有多种格式时按
   `png > jpg > jpeg > webp > avif > gif > svg` 取，**占位图一律排在最后**。
   这份清单必须**排在 `data.js` 之前**加载，构建时自动注入 `<script>`。
   构建还会把静态 HTML 里手写的图片路径（首页那几张卡、海报）按清单校正一遍。
2. **运行期兜底**：`main.js` 在捕获阶段接 `img` 的 `error`，按同一顺序换扩展名重试。
   清单过期时救场。

**日常操作**：盖掉同名文件 → 刷新页面。换成别的扩展名才需要双击 **`刷新图片.cmd`**。

> 首页那张海报 `assets/images/poster.*` 不在 slug 体系里（它不在任何板块目录下，
> 所以不进清单）。`build.ps1` 单独按 `png > jpg > …` 的顺序找 `poster.<ext>`，
> 再把 `home.html` 里那一行 `src` 对齐过去 —— 现在它是 `poster.jpg`。
> 早先这段是写死 `poster.png` 的，换成 jpg 之后会一边找不到、一边又生成一张
> 新的占位 png，所以改成了和封面同一套规则。

> 旧的 `AUTO_FIND_IMAGES`（本地预览时主动探测同名真图）**已经删掉**。
> 它存在的理由是「占位图和真图不同名，得去探一探」；现在同名，盖掉就生效，
> 没有可探的东西了，连带那一批探测 404 也一起没了。

### 3.5 分类可以「不生成页面，直接跳转」

分类对象加 `to: '<路径>'` 就行：

```js
{ slug: 'oc', label: L('原创角色', …), to: 'characters/', desc: L(…) }
```

这样导航、移动菜单、页脚、同级分类条里的这一项都指向 `to`，
`build.ps1` 不再为它生成分类页，**并且会把之前生成过的目录删掉**（不留孤儿页）。

目前**角色板块的两项都用了**，都指向同一个地方：

| 分类 | 简体标签 | 指向 |
|---|---|---|
| `oc` | 原创角色 | `characters/` |
| `character` | **角色**（原来叫「角色关系」） | `characters/` |

也就是说：点顶栏「角色」展开后，前两项「原创角色」「角色」都直接进那个
固定深色的选角色页，而不是先看一个文章列表。
（`character` 这一项的日文也跟着从「人物関係」改成了「キャラクター」——
标签已经不表示「关系」的意思了；英文保持 `Character`。）

> ⚠ 副作用：这两个分类下的 4 篇文章
> （`strawberry-sheet` / `oc-palette` / `character-relations` / `silence-design`）
> 都没有自己的列表页了。所以角色页底部有一栏「角色相关文章」，
> 收录**整个角色板块**的 7 篇文章，保证内容仍然在站内走得到。
> 改 `to` 的时候记得一并想这件事。

### 3.6 顶栏展开：六块面板共用一个「舞台」

用户反馈：从一个分类移到另一个分类时，面板会**先收起再展开**，很不流畅。

根因在旧实现的结构里：六块面板各自带着自己的白底，各自用 `clip-path` 做帘幕，
`show()` 里先 `hide()` 再显示新的 —— 横移必然经过「关掉 A、打开 B」两步，
中间那一帧塌下去，看起来就是闪一下。之前靠把动画时长缩到 .16s 掩盖，治标不治本。

现在改成 **openai / apple 的做法**：白底抽出来，做成一块共用的舞台
`.menu-stage`，六块面板装在它里面：

```html
<header class="hdr">
  <div class="hdr__inner">…导航项…</div>
  <div class="menu-stage" data-menu-stage>
    <div class="menu" id="menu-research">…</div>
    …其余五块…
  </div>
</header>
```

舞台 `height: 0; overflow: hidden`，高度由 JS 量好写进 `--menu-h`。
**帘幕效果就是这个高度动画本身**，面板不用再各自裁一遍。三种情形：

| 情形 | 舞台 | 面板 |
|---|---|---|
| 从无到有 | 高度 `0 → h`（.46s） | 内容自上而下淡入 |
| **面板横移** | 高度 `h₁ → h₂` 连续补间（.34s），**透明度一动不动** | 旧的按指针方向淡出去（.2s），新的从反方向接进来（.34s） |
| 彻底收起 | 高度 `→ 0` | 淡出 |

关键点：

- `show()` 里**不再调 `hide()`**。让位的那块走 `leave()`，两件事同时发生
- 横移方向由两个导航项的先后决定，写进 `--dx`：往右滑就往右送，方向感和鼠标一致
- **收起只看「指针有没有离开顶栏」**。面板是 `<header>` 的子节点，所以导航项之间、
  导航项和面板之间怎么走都不会触发 `mouseleave` —— 用户要的「没离开导航栏就别收起」
  由 DOM 结构本身保证，不靠计时器赌（只留 120ms 宽限，给擦边走位）
- 白底那道下边线用 `box-shadow: 0 1px 0` 画，不用 `border`：
  `border` 会算进 `border-box` 的高度，和量出来的内容高度差 1px，接缝会露出来
- 让位的面板被舞台的 `overflow: hidden` 裁住，所以**高矮面板互换时不会露在白底外面**
  —— 这正是把面板放进舞台里、而不是让舞台单做背景层的原因

实测（每帧采样 `getComputedStyle(stage).height`，见 §8 的验证方法）：

```
研究 → 作品   210 → 362  透明度全程 1.00，is-on 一次没掉，高度一次没回 0
作品 → 世界观 362 → 324  同上
```

---

## 4. 从 Figma 实测出来的关键数据

| 项目 | 值 | 来源 |
|---|---|---|
| 顶栏高 | 72px | 画板 1280×72 |
| 导航字号 | **17px** | openai.com 实测（原稿约 14px，按规范提高） |
| 导航项间距 | **43px** | 稿件路径坐标实测 42.9px |
| 下拉面板 | 背景**通栏**（1280×439，含顶栏）；内容块 **761 宽**，x 499→1260 | 「主页顶部栏展示」七张画板实测 |
| 展开内容左缘 | **499**，正好落在第一个导航项 Research（字形 x 510.6）上 | 同上 —— 所以实现里改成「量出第一个导航项」而不是写死 761 |
| 展开内容上下留白 | 上 32 / 下 72 | kicker y=104，面板底 439 |
| 展开列 | 4 列 × 160，第 1 列后留 50 间距 | 同上 |
| 展开遮罩 | `#9C9C9C` @ 68%（实现里收到 45%） | 画板上压的整幅矩形 |
| 卡片图比例 | **全部 1:1 正方形** | 稿件里图片占位框的统计：180² ×30、384² ×15、328² ×9 |
| 主按钮 | 188×52（圆角 26） | 稿件 |
| 次按钮 | 154×44 | 稿件 |
| 星体 | 420 正圆 / 2px 模糊 | 稿件 `<rect rx="210">`；质感照用户给的参考图重做（见 §6） |
| 内容边距 | 稿件 20px（1280 屏）；**本站是 `--gutter: clamp(20,3.2vw,40)`，1280 下算出 40** | 见 §9 待办 2 |

### 配色（全部来自 Figma 导出）

```
--text #1A1A1A   --text-muted #727272   --placeholder #EDEDED
--border #E6E6E6 --border-2 #CCCCCC     --bg-2 #F5F5F5
--star  #4BB6EC  ← 全稿唯一的彩色
```

> ⚠ `#6155F5` 是 **Figma 批注框底色**，不是品牌色。早期误当强调色用过，已纠正。
> 强调色 `--accent: #1F8FC9` 是由星体色加深得来（保证文字对比度过 AA）。

> ⚠ `--text-faint` 原稿是 `#9C9C9C`，白底对比度仅 **2.75**（12px 小字读不清），
> 已压到 `#767676`（4.58，刚过 AA）。层级改由字号承担。

---

## 5. Figma 批注（`说明文档` 图层）里的硬性规则

这些是设计者写在稿子里的要求，实现时必须遵守：

- 页脚（`Frame 52`）**每页常驻**，其中「顶」是面包屑，记录层级
- `View more` 按钮**同一页只出现一次**，放在右上角
- 文章页的**深色标签栏**由作者手动加标签 + 作者信息
- 「继续阅读」栏**自动放置同类型文章** —— 这个词**只在文章页用**，首页那一栏叫「最近更新」
- 导航跳转的二级页**统一用「通用版式」**，可按板块特性一对一调整
  （地图板块因更新慢，只留 3 列大板块，不要下方 6 个小板块）
- **角色页固定深色模式**，为了沉浸感；「查看资源」链到资源板块
- **「原创角色」和「角色」都不是文章列表**：这两项都直接跳到角色页 `characters/`（见 §3.5）；
  更早试过在分类页铺一排角色卡，用户否掉了 —— 不要中间那一层
- 立绘用 **PNG 透明背景**（现在这批是用户的三视图 JPG，等他自己换成透明 PNG）
- 卡片悬停有动画，点击进入对应文章

---

## 6. 用户明确的偏好

- **不要返回按钮**。apple.com / openai.com 都没有，完全依赖浏览器返回键 + 常驻导航
- 顶栏 **Apple 式常驻吸顶**（扉页除外 —— 全屏封面压条导航会破坏「翻开一本书」的感觉）
- **90% 符合设计稿即可**，字号等可按规范调整；排版尽量按稿子，但「规矩不是一成不变的」
- 用户非专业建站者，希望以资深建站师视角**主动提出更好的方案**，但**改动他的数据前要先问**
- 节日海报（16:9 全宽）**纯美学展示，刻意不可点击** —— 加跳转会和下方板块重复
- **文章正文不翻译**，保持写作时的原文（openai.com 同样如此）。只有界面/导航/标题/导语随语言切换
- 联系方式：**pengjasper@icloud.com**（真实邮箱）；X / Bluesky / Github 暂不做，显示「暂未开放」
- 角色页的「年份/版本/地图」是**筛选器**（用于查找角色），不是排序
- **UI 向 openai.com 靠拢，动画和交互向 apple.com 靠拢**（用户原话）
- **顶栏导航项右边不要向下的箭头**（Figma 稿里就没有）
- **从一个分类移到另一个分类时不许「收起再展开」**：指针没离开导航栏就一直开着，
  面板之间做丝滑切换（用户原话，参考 openai / apple）。实现见 §3.6
- **每张封面必须不一样**（2026-08-21）：「你之前填充的图片都是同一张，
  我自己想修改的时候无法分辨该改哪一张」。用户为此提供了 `网页图片/`，
  46 个 slug + 海报已经逐张换成语义对得上的照片，见 §3.4。
  （更早的偏好是「占位图直接用 PNG，这样我替换也方便」，那一轮已经过去了。）
- **翻译保持现状**（2026-08-21 明确）：简体导航就是中文，不改回 Figma 稿上的英文
- 展开面板的**内容左缘对齐第一个导航项**，左边那段留白是设计里就有的呼吸
- 「原创角色」和「角色」这两项**都不是文章选择界面**，点了直接进那个固定深色的选角色页
- **文章的内容和排版用户要能自己改**，包括直接在源码里写 HTML（见 §10）
- **悬停动画要更明显一点**（2026-08-21，原话「比如图片板块轻微上浮」）：
  卡片和列表的图片块 hover 时上浮 6px / 5px + 一层投影，图片本身放大到 1.05 / 1.06。
  只在 `(hover: hover) and (pointer: fine)` 下生效 —— 触屏上 `:hover` 会「粘住」，
  抬手之后还停在上浮态
- **切换语言直接整页重载**（2026-08-21，用户原话「可以设定切换语言后自动重新加载页面」）：
  见 §8 bug #21
- **移动端的语言选择器在右下角**，和 `JasperPeng©2026` 同一行（2026-08-21）
- **跨页不要过渡动画**（2026-08-21，用户反馈「加载新页面很卡，网页会闪烁跳动」）：
  见 §8 bug #19、#20
- 首页星体：质感照用户给的那张参考图 ——
  珠光皂泡感、边缘一圈亮环、内部彩光缓慢漂移并互相融合、一层细颗粒；
  动作要求「**小幅度缓慢上下沉浮**」+「**里面的光芒随机闪耀融合**」。
  实现：5 团彩光各带一组漂移 + 一组明灭动画，两组周期都互质（13/17/21/15/19 秒
  与 7.3/9.1/11.7/8.5/6.7 秒），叠起来看不出循环；沉浮 11s / -14px。
  更早参照过用户自己部署的 `jianghao.kdns.fr`（三段渐变 + `blur(2px)` + 6s 浮动），
  Figma 给的 420 正圆 / 2px 模糊也对得上。

---

## 7. 多语言系统

四语（简 / 繁 / 英 / 日）已完整回归测试过：
切换后残留中文只剩专有名词和语言选择器本身，都是有意保留的。

### 设计

`data.js` 里用两个构造器：

```js
L(简, 繁, En, 日)   // 四语字段
P(专有名词)          // 四语同形，天然不翻译
```

**专有名词不翻译**：`JasperPeng` `嗵嗵仮面` `NooteMetro` `DOUDOU` `草莓酱/拉/祈/夏花/嗵嗵/Noote`

**翻译范围**：板块名、分类名、板块标题/导语、文章标题/导语、角色角色描述/台词/简介、全部 UI 词。
**不翻译**：文章正文（`body`）—— 保持原文，与 openai.com 一致。

**日期**不写死，存 ISO 值，由 `Intl.DateTimeFormat` 按语言格式化。
**阅读时长**存数字，由 `READ_FMT` 按语言拼量词。

### 静态 HTML 如何跟着切换

页头页脚是构建期生成的静态 HTML（写入简体文案），但每个标签带路径：

```html
<button class="nav__link" data-t="s.works.label">作品</button>
<a data-t="c.works.artifacts.label">手作</a>
<span data-t="l.contact.1.label">X</span>
```

`main.js` 的 `resolvePath()` 解析这些路径，切换语言时**只替换文本节点**，
保留同级的图标元素（下拉里的 `.arr` 箭头等）。顶栏导航项本身现在没有图标。

路径语法：

| 前缀 | 含义 | 例 |
|---|---|---|
| `s.<key>.<字段>` | 板块字段 | `s.works.label` / `s.works.kicker` |
| `c.<key>.<slug>.<字段>` | 分类字段 | `c.works.artifacts.desc` |
| `f.<key>.<序号>.label` | 下拉推荐位 | `f.works.0.label` |
| `g.<key>.<序号>.label` / `gk.<key>` | 标签胶囊 / 其标题 | `g.works.0.label` |
| `l.<key>.<序号>.label` | 联系方式链接 | `l.contact.1.label` |
| `n.<key>.<序号>.<字段>` | 说明块 | `n.contact.0.text` |
| `ui.<键>` | 纯 UI 词 | 也可用 `data-i18n="<键>"` |

首页的静态卡片挂 `data-slug="<文章 slug>"`，切换语言时由 `rerenderStaticCards()` 按 slug 重写标题和元信息。

---

## 8. 已修复的 bug（保留记录，避免重蹈覆辙）

| # | Bug | 根因 |
|---|---|---|
| 1 | **全站点不动**（最严重） | `.search-overlay { display: grid }` 盖过浏览器默认的 `[hidden]{display:none}`，一层透明遮罩长期铺满全屏拦截所有点击。修复：`[hidden] { display: none !important }` |
| 2 | 首屏文字不显示、整个 main.js 抛错 | `t()` 在 `I18N` 赋值前被搜索模块调用 —— 变量提升只提升声明不提升赋值。修复：把语言表移到工具函数之后 |
| 3 | 悬停展开后点击反而关闭下拉 | 用户会以为「点了没反应」。改成 hover 预览 + click 固定，第二次点击才收起 |
| 4 | 1024px 下星体外晕溢出视口 | 收窄星体给外晕留位；用 `overflow-x: clip` 而非 `hidden`（`hidden` 会让根元素变成滚动容器、废掉页头 sticky） |
| 5 | 角色筛选器点了没反应 | 做成了排序，而数据本来就按年份排列。改成两级筛选：选维度 → 出取值胶囊 → 筛出子集 |
| 6 | `<h3>` 嵌在 `<span>` 里 | 无效 HTML。改为 `<div>` / `<p>` |
| 7 | 分类页 canonical 404 | 用了板块 key（`about`）而不是目录名（`resources`）。模板加 `{{DIR}}` 令牌 |
| 8 | 切英文后残留 24 处中文 | 只做了 UI 词翻译，内容层没做。已重建整套 i18n（见 §7） |
| 9 | **文章页正文整块空白**（最严重的遗留 bug） | `renderArticle()` 里用了一个从没定义过的 `note` 变量 → `ReferenceError` 抛在 `host.innerHTML =` 上，整个 main.js 的 IIFE 中断：正文、相关阅读、连启动时的 `i18n()` 全都没跑。**上一版 README 说「各页面无控制台报错」是错的**。修复：把 `note` 按原意补成「界面语言 ≠ 正文语言」的提示（`t('bodyOriginal')`） |
| 10 | 角色缩略图选中框上半截被切 | `.chars__strip` 是 `overflow-x: auto`，而 `overflow-x` 一旦不是 `visible`，`overflow-y` 也会被算成 `auto` —— 上下同样会裁。原来只有 `padding-bottom`。修复：四周留白 + 选中环从 `outline` 换成 `box-shadow` |
| 11 | 星体的指针牵引其实一直没生效 | 牵引（内联 `style.transform`）和呼吸（CSS 动画的 `transform`）写在同一个元素上，**CSS 动画优先级高于内联样式**，牵引被整个吃掉。修复：牵引挂 `.star`，呼吸挂 `.star__body` |
| 12 | 角色缩略图 `aria-label` 是 `[object Object]` | `esc(c.name)` 直接拼了四语对象。修复：`esc(T(c.name))` |
| 13 | 换图之后首页那几张卡断链 | 首页的卡片图是**手写**在 `home.html` 里的，不走清单。占位图一改名就全断。修复：构建时按清单把静态 HTML 里的 `assets/images/...` 路径也校正一遍 |
| 14 | 自动探测新图刷了满屏 404 红字 | `new Image()` 加载失败会写控制台；换成 `fetch(HEAD)` 也一样（Chrome 对 fetch 的 404 同样记 console error）。结论：探测天然有噪音。**现在整个 `AUTO_FIND_IMAGES` 已删除** —— 占位图改成 `<slug>.png` 之后，盖掉就生效，没有可探的东西了 |
| 15 | 角色页「地图」筛选渲染成 `[object Object]`，而且筛出来永远只剩 1 个 | `distinct()` 把取值直接当字符串用。`year` / `version` 是字符串没事，`region` 是 `L()` 生成的四语**对象** —— `esc(v)` 得到 `[object Object]`，`seen[v]` 又把所有地图并成同一个键。修复：显示走 `T(v)`，比较走一个跟语言无关的稳定键 `valKey()`（四语对象取简体那一支）。和 bug #12 同源，那次只修了 `aria-label` 一处 |
| 16 | 换一次语言之后，角色页筛选「点了没反应」 | `rerender()` 换语言时会再调一次 `renderCharacters()`，而它每次都重新 `addEventListener`。叠两层之后点取值胶囊等于点了两下，`pick` 立刻被自己取消掉。顺带 `mode` / `pick` 也被重置。修复：状态提到函数外（`charMode` / `charPick`），事件用 `charsBound` 标记只绑一次 |
| 17 | 顶栏面板之间横移会先收起再展开 | 见 §3.6。旧实现里每块面板各带白底、各自 `clip-path`，`show()` 又是「先 hide 再 show」，横移必然塌一帧。改成六块面板共用一个高度可补间的舞台 |
| 18 | **手机上首页星体露出一圈矩形边界**（深色下尤其明显） | 圆边界原来靠 `.star__body` 的 `overflow: hidden` + `border-radius: 50%` 裁。移动端 Safari 在「元素同时有 `filter: blur()` 和 3D 变换动画」时会**丢掉圆角、只按矩形 border-box 裁**，于是球的底色（深色下是 `#23222A→#141419`，比页面底色亮）就以方块的形状糊在黑底上。修复两层：① 改用 `mask-image: radial-gradient(circle closest-side …)` 画圆边界 —— mask 在 filter 之后应用，不会被跳过；② 底色渐变本身也改成 `closest-side` 且边缘收到透明，就算 mask 也失效，露出来的仍是圆 |
| 19 | **每次点链接都要白等 240ms**（用户说「卡」） | `main.js` 的 `pageTransition` 先给 `body` 加 `is-exiting` 淡出，`setTimeout` 240ms 之后才真跳转。不支持 View Transitions 的浏览器（用户的手机）全走这条。**整个 `pageTransition` 已删除**，点了立刻走 |
| 20 | **换页时闪烁跳动** | 两个来源叠在一起：① CSS 的 `@view-transition { navigation: auto }` —— 跨文档视图过渡会冻住旧页截图，再把「JS 还没渲染完」的新页换上去，先闪一下空白；② `.reveal` 的入场动画交给 `IntersectionObserver`，而它的回调要等**第一帧画完之后**才跑，所以首屏内容是「先空白、再淡进来」。修复：删掉 `@view-transition`；`reveal()` 里同步判一次首屏（`getBoundingClientRect().top < innerHeight`），首屏那批**关掉 transition 再加 `is-in`**，等画过一帧（双层 `rAF`）再把 transition 还回去 —— 首屏直接就位、不动画，往下滚出现的照旧有入场动画 |
| 21 | **换语言之后有些块消失，得手动刷新才回来** | 各页的渲染函数不是幂等的：`rerender()` 第二次调用 `renderArticle()` / `renderCategory()` / `renderCharacters()` 时，某些块（相关阅读、角色筛选器）会被重复绑定或重复清空。一处一处补容易再漏，**改成直接 `location.reload()`** —— 语言存在 `localStorage`，查询串（`?a=` / `?c=`）由 reload 原样保留，滚动位置浏览器自己恢复。`localStorage` 被禁（无痕模式）时存不住，那条路退回原来的就地重画 |
| 22 | 简体首页的卡片元信息是中英混排（`News · 2026年8月18日 · 8 min read`） | 首页那几张卡是**手写在 `home.html` 里**的静态 HTML，元信息以前只在「换语言」时才由 `rerenderStaticCards()` 重写，首次打开就保持源码里那份英文。修复：启动时也走一遍 `rerenderStaticCards()` |

### ⚠ 测试方法论（重要教训）

**不要用 `element.click()` 验证交互** —— 它直接派发到元素，**绕过命中测试**。
Bug #1 就是这样躲过我全部自动化测试的：测试全绿，真实鼠标一个都点不到。

正确做法：
- 用真实坐标点击（`computer` 工具的 `left_click`）
- 用 `document.elementFromPoint(x, y)` 做命中测试
- 用真实 HTTP 服务器测（预览面板会把页面转成 `data:` URI，相对路径全失效 —— 那是**预览器行为**，不是站点 bug）

**查「JS 生成的链接」要用 iframe 批量跑**（只扫 HTML 文件查不全，站里大部分链接是运行时拼的）：
把每个页面装进一个 iframe，等 JS 渲染完再收 `a[href]`，然后逐个 HTTP 校验。
iframe **必须加 `sandbox="allow-same-origin allow-scripts"`** ——
不加的话子页面会把外层窗口一起带走，测试跑一半就断了（踩过两次）。

**`iframe.onload` 会先为初始的 `about:blank` 触发一次**，不判一下就直接量，
量到的是空文档（这次因此得出「0 个 `.reveal`」，白折腾了一轮）：

```js
f.onload = () => { if (f.contentDocument.location.href === 'about:blank') return; … };
f.src = url; document.body.appendChild(f);
```

**一次别跑太多页**：67 个页面 × 每页几百毫秒会超过工具的单次执行上限。
把状态挂在 `window` 上、分批调用（`window.__sweep(25)`）就不会中途被掐断。

**要量「首屏有没有闪一下」，iframe 不能藏在 `left:-9999px`** ——
面板不显示时浏览器不合成帧，过渡不推进，读到的永远是起点值。
用 `opacity: .01; z-index: -1; pointer-events: none` 让它真的在视口里画。

**验动画不能只看首尾状态**，要每帧采样。判断「横移到底有没有塌一下」就是靠
`requestAnimationFrame` 里连续记 `getComputedStyle(stage).height` / `opacity` / `className`，
再回看那 300ms 的窗口 —— 光看动画结束后的静态值，塌不塌根本看不出来。

**扫死代码**（每轮收尾跑一遍，很便宜）：

```bash
# 未使用的 class：CSS 里声明了，但 HTML / JS / build.ps1 里一次都没出现
grep -oE '\.[a-zA-Z][a-zA-Z0-9_-]*' css/style.css | sed 's/^\.//' | sort -u > /tmp/cls.txt
cat *.html */*.html */*/*.html js/*.js build.ps1 > /tmp/all.txt
while read c; do grep -q -- "$c" /tmp/all.txt || echo "UNUSED .$c"; done < /tmp/cls.txt
```

同样的思路再跑三遍：CSS 自定义属性（比 `var(--x)` 的出现次数）、
JS 函数名（出现 1 次 = 只有定义、没人调用；IIFE 的名字是例外）、
`data.js` 的字段名（在 main.js + build.ps1 里 grep）。
**注意两类假阳性**：只出现在注释里的（比如 `.search-overlay`），
以及 `$` / `$$` 这种正则里 `\b` 匹配不了的名字。

**面板高度差 1px 会露缝**：白底和面板内容的高度必须严丝合缝。
`border-bottom` 会算进 `border-box` 高度，用 `box-shadow: 0 1px 0` 画那道线才不会差。

本地测试服务器：`本地预览.ps1`（PowerShell HttpListener，监听 5599），
行为与 Live Server 一致 —— 目录请求返回 `index.html`。用户日常用 Live Server，
这个脚本是给自动化验证用的：

```bash
powershell -ExecutionPolicy Bypass -File 本地预览.ps1
```

> 之前这份脚本放在会话临时目录里，换一次会话路径就变了。现在收进项目根目录。

**验证时的坑**：浏览器面板不显示时**不合成帧，CSS 过渡不会推进**，
`getComputedStyle` 读到的永远是过渡起点、截图也拿不到。
要看「最终态」就先临时注入 `*{transition:none!important}` 再量。

---

## 9. 当前状态

### 已完成并验证

- **22 个 HTML 页面**：18 个分类页 + 扉页 / 首页 / 文章 / 角色，另加 `category.html` 模板
  （构建时「注入页头页脚 21 个」是对的 —— 扉页没有页头页脚标记。
  上一版是 23 / 19 / 22，少的那个是被删掉的 `characters/character/`）
- 静态断链 **0**（脚本逐文件解析 `href` / `src` 核对过，1828 条）
- **没有孤儿页**：没有任何 HTML 是「存在但无人链接」的
- 1280 / 975 / 780 / 430 / 375 各档**无横向溢出**
- 深色模式（跟随系统）+ 角色页深色锁定
- 各页面重新加载**无控制台报错**（67 个页面逐个装进 iframe 跑过，0 条 error）
- 47 个图片文件（46 个 slug + `poster.jpg`，5.4MB），**张张不同**、全部真实文件、无 `data:` 内联

**最新一轮（2026-08-21，用户提的 7 条）已做并验证：**

| 需求 | 做法 | 验证 |
|---|---|---|
| ①「角色」分类中文改名并跳角色页，删掉原来那个页面 | `data.js` 里 `character` 分类：`label` 简/繁改成「角色」（日文一并改成「キャラクター」），加 `to: 'characters/'`。`build.ps1` 检测到带 `to` 就不生成、并删掉旧目录 | 顶栏下拉 / 移动手风琴 / 页脚 / 同级分类条**四处**都指向 `characters/`；真实指针点「角色」落到深色角色页；`characters/character/` 已删；全站 0 孤儿页 0 断链 |
| ② 移动端星体露出矩形边界 | `mask-image` 画圆 + 底色渐变收成 `closest-side` 且边缘透明（见 §8 bug #18） | 375 深色下截图确认是正圆，无方块；1280 浅色下珠光质感不变 |
| ③ 加载新页面卡、闪烁跳动 | 删掉 240ms 淡出跳转 + 删掉 `@view-transition` + 首屏 `.reveal` 同步就位不做动画（见 §8 bug #19、#20） | 67 个页面在 `load` 那一刻采样：首屏 `.reveal` **全部 `is-in`、opacity 1、transition none**；400ms 后 transition 已还原、首屏之下的 15 个仍 opacity 0 等着滚动触发 |
| ④ 移动端语言选择器移到右下角 | 去掉 ≤640 的 `.ftr__bar { flex-direction: column }`，改成 `nowrap` 一行；`.ftr` 底边加到 84px 给固定的回顶按钮让位 | 375 实测：版权 x=20（左）、语言按钮 right=355（右），同一行居中对齐，`scrollWidth == clientWidth` 无溢出；回顶按钮不再压住语言按钮 |
| ⑤ 切换语言后自动重载 | 选中语言 → 写 `localStorage` → `location.reload()`（见 §8 bug #21） | 在 `characters/?c=qi` 上切到日文：URL 与选中角色都保留，7 个缩略图、7 条相关文章一个不少；再点「地域」筛选 → 出 7 个地域胶囊（日文、无 `[object Object]`）→ 点「旧港」筛出 1 个。README bug #15/#16 那两个老毛病也一并不会再出现 |
| ⑥ 悬停动画更明显 | `.card__media` / `.list__media` hover 上浮 6px / 5px + 投影，图片放大到 1.05 / 1.06，只在 `(hover:hover)` 下生效 | 真实指针悬停后 computed：`matrix(1,0,0,1,0,-6)` + `box-shadow` + 图片 `matrix(1.05,…)` |
| ⑦ 每张封面换成不一样的图 | 从 `网页图片/` 的 85 张素材里逐个挑，居中裁 800×800（立绘保原比例），存 JPEG q84（见 §3.4） | 46 张封面**逐张算 SHA256，无一重复**；47 个文件全部存在且都在清单里；67 个页面 0 张裂图；总体积 14.1MB → 5.4MB |
| （追加）通达性 + 死代码 | 从扉页做 BFS 遍历确认每页真的走得到；四类死代码各扫一遍后清掉（见下面两小节） | 67/67 可达、0 死胡同、最深 3 次点击；清理后未使用的 class / 变量 / 函数均为 0 |

**上一轮（顶栏横移动画 + PNG 占位图 + 全站跳转排查）也还在，仅供追溯：**

| 需求 | 做法 | 验证 |
|---|---|---|
| 面板之间横移不要「收起再展开」 | 六块面板装进共用舞台 `.menu-stage`，横移只补间高度；`show()` 不再先 `hide()`（见 §3.6） | 每帧采样舞台高度：研究→作品 210→362、作品→世界观 362→324，**全程 opacity 1.00、`is-on` 没掉、高度没回过 0** |
| 没离开导航栏就不要收起 | 收起改成只听 `header` 的 `mouseleave`；面板本来就是 header 的子节点，所以导航项之间、导航项↔面板之间怎么走都不触发 | 真实指针从导航项走进面板里的「地图」链接（`pointerover <A> 地图`），面板一直开着；移出顶栏后 h=0 / opacity 0 / scrim 关闭 / 六个 `aria-expanded` 全 false |
| 直接用 PNG 占位，方便替换 | 占位图改成真实的 `<slug>.png`，图源取自 Figma 稿里内嵌的三张照片；靠 SHA256 登记表区分占位图和真图（见 §3.4） | 「直接盖掉 `<slug>.png`」和「另丢一张 `.jpg`」两条路都实测过：前者清单认成真图、路径不变；后者 jpg 压过占位 png，首页手写的 `src` 也跟着校正 |
| 每个窗口的跳转一一对应 | 见下方「跳转排查」 | 静态 1914 条 + 运行期 68 个页面全部命中，0 断链 0 孤儿页 |
| 顶栏不要向下箭头 | `build.ps1` 生成 `nav__link` 时不再输出 `<i class="chev">`，CSS 里那条规则也删了 | 页面上 `.nav__link .chev` 数量 = 0 |
| 展开栏左缘对齐第一个导航项 | 背景通栏，内容块 `margin-left: var(--menu-left)`；`alignMenus()` 运行时量出第一个导航项的 left 写进 `--menu-left`，resize / 换语言都重量 | 六个面板内容左缘都 == 第一个导航项左缘；简/英/日三种语言分别 717 / 544 / 462，全部吻合 |
| 「原创角色」不再是文章列表 | 该分类加 `to: 'characters/'`（见 §3.5），导航/移动菜单/页脚/同级分类条全部指向深色角色页，`characters/oc/` 不再生成并已删除 | 四处链接实测都是 `characters/`；`characters/oc/` 返回 404 且无人链接 |
| 不要留下垃圾页面 | 构建时检测到带 `to` 的分类若有旧目录就删掉 | 断链 0、孤儿页 0 |
| 那两篇文章别掉出站点 | 角色页底部新增「角色相关文章」栏，列出整个角色板块的 7 篇 | 7 条链接全部可达 |
| PNG / JPG 换图 | 清单按格式优先级取；构建还会校正静态 HTML 里手写的图片路径（占位图命名已在上面那一轮改成 `<slug>.png`） | 真的放了透明 PNG（600×800 立绘）、JPG、以及「**不跑构建**直接丢 PNG」三种情况，全部正确显示 |
| 正文能自己写 | `body` 支持 `p/h2/h3/quote/ul/ol/hr/img/figure/html` 十种块，也支持整篇写成 HTML 字符串（见 §10） | 十种块逐个渲染检查过：转义正确、原样 HTML 生效、自定义 `src` 生效 |
| 星体质感 | 见 §6 | computed style 确认 5 团光 + 10 组动画（5 漂移 + 5 明灭）+ `blur(2px)` + 亮环 + 颗粒都在 |

### 跳转排查（「不能出现跳转失灵或跳转错误」）

分两层查，因为站里大部分链接是 **JS 运行时拼出来的**，只扫 HTML 文件是查不全的。

**第一层：静态。** 逐文件解析 `href` / `src`，规整路径后核对文件是否存在，
查询串也要对得上（`?a=` 必须是真实文章 slug，`?c=` 必须是真实角色 slug）：

```
data.js: 文章 39 篇，角色 7 个
静态链接：检查 1828 条
OK  静态 href / src 全部命中，查询串一一对应
OK  没有孤儿页            OK  分类与目录一一对应（18 个）
OK  图片清单 46 条全部存在  OK  每个 slug 都在图片清单里
OK  46 张封面互不相同
（`characters/oc` 与 `characters/character` 下那 4 篇没有自己的分类列表页 —— 这是
  §3.5 设计如此，由角色页底部的「角色相关文章」兜底，见下面的运行时验证）
```

> 检查器本身有两个坑，写新的时候别再踩：`category.html` 是含 `{{TOKEN}}` 的模板，
> 必须排除掉；`href` 解析出来如果指向一个**目录**（`works/artifacts`），
> 要补 `/index.html` 之后再登记，否则孤儿页检查会把所有分类页都误报一遍。

**第二层：运行时。** 把 67 个页面（扉页 / 首页 / 18 个分类 / 角色页 + 7 个角色 /
39 篇文章）逐个装进一个 `sandbox="allow-same-origin allow-scripts"` 的 iframe，
等 JS 渲染完再收集 `a[href]`，然后逐个 HTTP 校验：

```
67/67 页渲染成功，68 个唯一目标全部 200
0 断链  0 报错  0 处 [object Object]  0 张裂图  没有空白页
0 处中英混排（扫 "min read"）  首屏 .reveal 全部已就位
```

（用 sandbox 是因为不加的话子页面会把外层一起带走，测试跑一半就断了。）

> 又一个坑：`iframe.onload` 会**先为初始的 `about:blank` 文档触发一次**。
> 不判 `contentDocument.location.href !== 'about:blank'` 就直接量，量到的是空文档
> （这次一开始就量出「0 个 `.reveal`」，白折腾了一轮）。
>
> 还有：`index.html`（扉页）**不加载 `main.js`**，它有自己的内联脚本。
> 拿 `html.js-on` 当「脚本跑完了没有」的判据时要把扉页排除，否则一定误报。

**覆盖率反查**：39 篇文章、18 个分类、7 个角色的目标 URL 全部出现在链接集合里。
角色里 `tong` / `doudou` / `noote` 没有 `?c=` 形式的 `<a>` —— 这是对的，
它们通过角色页的缩略图条（`<button>` + `pushState`）进入，7 个缩略图逐个
`elementFromPoint` 命中自己，不是被谁盖住的。

**第三层：通达性（从扉页真的走得到吗）。**
前两层只回答「链接指向的东西存在吗」，回答不了「用户点得到吗」。
所以又做了一次**从 `index.html` 出发的广度优先遍历** —— 每个页面装进 iframe、
等 JS 渲染完，收它的 `a[href]`，再把新目标排进队列：

```
从扉页出发可达 64 个页面（纯 <a> 链接）
点击深度：扉页 0 → 首页 1 → 37 个页面 2 → 25 个页面 3
最深 3 次点击到达任意一页
0 死胡同（每页出链都 ≥ 3 条）
64/64 页都有回首页的链接
64/64 页都能直接到六个板块（顶栏常驻，二级页也一样）
```

差的 3 个是 `?c=tong` / `?c=doudou` / `?c=noote` —— 它们**没有 `<a>` 形式的入口**，
要通过角色页的缩略图条（`<button>` + `pushState`）进入。这是设计如此，不是断路：
7 个缩略图逐个 `elementFromPoint` 命中自己，真实指针点第 7 个（Noote）后
URL 变成 `?c=noote`、标题和立绘都跟着换。**加上这条，67/67 全部可达。**

唯一一条「只有一个入口」的是 `article.html?a=paper-strip` ——
它是 `works/comics` 里唯一的一篇，所以别的文章的「继续阅读」带不到它。
结构使然，不是 bug；那个分类多一篇文章就自然解决。

**真实指针验证**（README §8 的方法论，不用 `element.click()`）：

- 在 `works/artifacts/`（二级目录，base 是 `../../`）悬停「世界观」→ 面板展开、
  内容左缘对齐第一个导航项 → 真实坐标点「地图」→ 落到 `universe/maps/`，标题正确
- 搜索「草莓」→ 3 条结果，`../../` 前缀正确、缩略图正确，三条都 `elementFromPoint` 命中自己
- 375px 下：舞台 `display: none`、无横向溢出、汉堡可点、手风琴展开 304px，
  6 条链接逐个命中自己

本轮（2026-08-21）新加的真实指针验证：

- 顶栏「角色」展开 → 点第二项「角色」→ 落到 `characters/`，标题「草莓酱 — 角色」
- 780 宽下：汉堡 → 手风琴「角色」展开 → 点「角色」→ 同样落到 `characters/`
- 页脚语言按钮 → 菜单展开 → 点「日本語 日本」→ 整页重载，`?c=qi` 保住
- 角色页「地域」筛选 → 出 7 个日文地域胶囊 → 点「旧港」→ 筛出 1 个
- 悬停第一张卡 → `translateY(-6px)` + 投影 + 图片 `scale(1.05)`
- 搜索「草莓」→ 3 条结果，缩略图各不相同

> ⚠ 工具坑：**浏览器面板的点击偶尔会卡住**（只发出 `pointerdown` / `mousedown`，
> 永远等不到 `mouseup` / `click`，工具本身 30s 超时）。这不是站点的问题 ——
> 用 `document.elementFromPoint()` 反复确认过命中的就是目标元素。
> 解法是**开一个新标签页**，在新标签里点击立刻恢复正常。
> 另外 `resize_window` 的 mobile 预设（以及任何 <768 的宽度）会打开触屏模拟，
> 那时同样只发 `pointerdown` / `touchstart`。要用真实指针测窄屏，
> 把宽度设成 **780**（<900 出汉堡、≥768 不进触屏模拟）最稳。

### 死代码清理（2026-08-21）

按「CSS 选择器 / CSS 变量 / JS 函数 / data.js 字段」四类各扫了一遍，
**声明了但全站一次都没用到**的都删了。记在这里是为了万一想用回来：

| 删掉的东西 | 原来在哪 | 说明 |
|---|---|---|
| `.visually-hidden` | 02 复位 | 屏幕阅读器专用的隐藏工具类，没有任何元素用它（跳过链接走的是 `.skip-link`） |
| `.sec-foot` | 04 排版 | 段落底部居中容器 |
| `.btn--sm` / `.btn--block` | 05 按钮 | 次按钮尺寸（Figma 154×44）和满宽按钮变体。**Figma 的次按钮数值现在只剩 README §4 那张表记着** |
| `.arr-l` + `--m-arl` | 05 图标 | 向左的箭头 mask，全站只用向右的 `.arr` |
| `.txt-link` | 05 图标 / 21 响应式 | 只在两条组合选择器里被捎带，没有元素用 |
| `.grid--4` | 12 卡片 + 三处响应式 | 四列栅格。删掉后 `@media (max-width:1180px)` 整块空了，也一并删了 |
| `.cats__link.is-active` | 14 分类导航 | `aria-current="page"` 的同义别名，JS 只写 `aria-current` |
| `--shadow-sm` | 01 令牌（浅色 + 深色各一处） | 从没被 `var()` 引用过 |
| `--sp-xs: 6px` | 01 令牌 | 同上 |
| `charUrl()` | main.js | 拼 `characters/?c=` 的函数，定义了但没人调用（角色卡走的是 `charArt` + `pushState`） |
| `landing: true` | data.js 角色板块 | 字段名还在，但 main.js / build.ps1 都不读它 |
| `external: true` | data.js 三条联系方式 | 同上，「暂未开放」的渲染只看 `soon` |

保留但确认过的：`.search-overlay` 只出现在**注释**里（记录 bug #1 的成因），不是死规则。

清理后再扫一遍：**未使用的 class 0 个、未使用的 CSS 变量 0 个、未调用的函数 0 个**，
CSS 花括号 389/389 配平。

### 🔧 未完成 / 待验证

1. **星体质感这轮终于看到了**（截图确认：正圆、珠光、内部彩光都在，深色下也没方块）。
   但**手机上的真机效果仍未验证** —— bug #18 那个圆角丢失是移动端 Safari 特有的，
   Chromium 复现不出来，只能靠 mask 这条更稳的路兜住。
   **建议在自己手机上再看一眼首页星体**。要微调的话仍是这几个数：
   - `.star__body i { opacity }` 由 `sh1…sh5` 控制（现在在 .28–.82 之间起伏）—— 彩光的存在感
   - `.star__body::before { opacity }`（现 `.5`）—— 颗粒的粗细
   - `.star__body::after` 里 93% 那一段的白色透明度（现 `.92`）—— 亮环的强弱
   - `@keyframes orbFloat` 的 `-14px` / `11s` —— 沉浮幅度与快慢

2. **展开面板右缘对的是本站版心（gutter 40），不是 Figma 的 20**。
   Figma 在 1280 下右边距 20、内容宽 761；本站 gutter 是 `clamp(20,3.2vw,40)`，
   1280 下算出来 40，所以内容宽 681。左缘完全吻合（这是用户明确要求的那一条），
   右缘跟着全站版心走。要完全复刻 Figma 就得把 `--gutter` 改成 20，但那会影响所有页面。

3. ~~导航默认语言的取舍~~ —— **已定**：2026-08-21 用户明确「翻译问题保持现状就好」。
   简体模式下导航就是「研究/作品/世界观/角色/关于/联系」，不再改成 Figma 稿上的英文。
   真要改的话仍然是改 `data.js` 里各 section `label` 的 `zh-Hans` 值（一处一行）。

4. 39 篇文章中只有 `indian-anklets` 是 Figma 原文，其余标题多取自图层名、**正文仍是占位创作**。
   封面已经全部换成用户自己的照片（§3.4），但**文字还等着他自己写**。

5. **7 张角色立绘现在是三视图 JPG，底色是灰/白的**。角色页是深色，
   `object-fit: contain` 会把这块浅色矩形整个露出来。Figma 批注要求的是
   **PNG 透明背景**，等用户换成去背的 PNG 就自然解决；代码这边不用动。

6. **`网页图片/` 有 248MB，不要跟着部署上线**。它只是素材库，站点本身不引用它
   （`assets/images/` 里那 5.4MB 才是站点用的）。要发布的话把这个目录排除掉。

---

## 10. 日常维护

### 加一篇文章 / 自己写正文和排版

往 `js/data.js` 的 `ARTICLES` 加一条，填对 `section` / `cat`，标题导语用 `L()` 四语：

```js
add({ slug: 'my-slug', section: 'works', cat: 'artifacts', date: '2026-09-01', read: 5,
  title: L('中文标题','繁體標題','English title','日本語タイトル'),
  lede:  L('…','…','…','…'),
  tags: [L('作品','作品','Works','ワークス')],
  body: [ { t:'p', v:'正文…' } ]   // 正文保持原文，不翻译
});
```

首页、分类页、相关阅读、搜索会自动带上。**不用改任何 HTML。**

**正文 `body` 可以怎么写**（想怎么排版就怎么排）：

```js
body: [
  { t:'p',      v:'段落' },
  { t:'h2',     v:'小标题' },
  { t:'h3',     v:'更小的标题' },
  { t:'quote',  v:'引用' },
  { t:'ul',     v:['第一条','第二条'] },        // t:'ol' 是有序列表
  { t:'hr' },                                   // 分隔线
  { t:'img',    src:'assets/images/works/foo.png', cap:'图注', alt:'替代文字' },
  { t:'figure', cap:'不给 src 就用这篇文章的封面' },
  { t:'html',   v:'<div class="随便什么">这一段原样输出，不转义</div>' }
]
```

想完全自己控排版，整篇直接写 HTML 也行：

```js
body: '<p>第一段</p><h2>标题</h2><div class="my-layout">…</div>'
```

> 除了 `html` 块和「整篇字符串」这两种，其余都会自动转义 ——
> 正文里写 `<` `>` `&` 不会出事。反过来，用 `html` 时内容由你自己负责。
> 字符串形式还能塞 `<img src="assets/images/…">`，路径从**站点根目录**算起
> （文章页在根目录，所以直接写 `assets/…` 就对）。

### 加分类 / 改模板 / 改头尾

```bash
powershell -ExecutionPolicy Bypass -File build.ps1
```

> ⚠ `build.ps1` 和 `serve.ps1` 必须存为 **UTF-8 with BOM**。
> Windows PowerShell 5.1 读无 BOM 的中文会当成 ANSI，直接解析报错。

### 换真实图片

现在 46 个 slug + 海报每张图都不一样，所以**先在页面上看到哪张想换，
再去 `assets/images/` 里找同名的那个文件**就行 —— 名字和你看到的内容对得上。

**最省事的一条路（推荐）：**

1. 找到 `assets/images/{板块目录}/{slug}.jpg`（少数是 `.png`，见下面那张表）
2. 用自己的图**存成同一个文件名、同一个扩展名，盖掉它**
3. 刷新页面

就这三步。**不用删任何文件、不用改任何代码，连构建都不用跑** ——
页面上的 `src` 一直指着这个名字。

当前哪些是 `.png`（其余全是 `.jpg`）：

| 文件 | 为什么是 png |
|---|---|
| `assets/images/characters/la.png` | 源素材 `拉设定3视图.png` 本来就是 png |
| `assets/images/universe/noote-metro.png` | 源素材 `SHUNIU (1).PNG` 是线稿图，png 更清楚 |

角色立绘同理，盖掉 `assets/images/characters/{slug}.jpg` 即可。
Figma 批注要求 **PNG 透明背景** —— 换成 `<slug>.png` 更好（png 在清单里压过 jpg，
但**记得把旧的 jpg 删掉**，不然目录里会同时躺着两张，容易看花眼）。
角色页用 `object-fit: contain`，不会裁掉边。

**想换成别的扩展名**：把 `<slug>.png` / `.webp` 丢进同一个目录，
然后双击根目录的 **`刷新图片.cmd`**（就是跑一次 `build.ps1`），让清单重排一次序。

支持的扩展名，优先级从高到低：
`png` > `jpg` > `jpeg` > `webp` > `avif` > `gif` > `svg`，
**占位图（哈希还对得上的那些）一律排在最后** —— 不过现在一张占位图都没有了。

- 透明 PNG：卡片是 `object-fit: cover`，透明处会露出底色 `--placeholder`；立绘不裁切
- JPG：想要轻量就用它。同一 slug 里 png 压过 jpg

> 首页那张海报是 `assets/images/poster.jpg`（16:9），不在 slug 体系里，
> 但同样是「盖掉同名文件」就生效；构建时也会顺手把 `home.html` 里那一行 `src`
> 对齐到实际存在的那个扩展名。

> **想重新生成占位图**：删掉某个 slug 的所有图，再跑一次 `build.ps1`，
> 脚本会从 `my web figma.svg` 里取图现补一张，并把它登记进 `.placeholders.txt`。
> （这套机制现在没有实例在用，但留着，新增 slug 时不至于开天窗。）

---

## 11. PowerShell 踩坑记录

- **负索引回绕**：`$arr[0..-1]` 在数组只剩 1 个元素时会**反向返回整个数组**，
  不是空数组。写路径规整（消解 `..`）时被这个坑过两次，导致链接检查器误报 200 条断链。
  改用 `System.Collections.Generic.List` + `RemoveAt()`。
- **zip 反斜杠**：`Compress-Archive` 和 .NET Framework 的 `ZipFile.CreateFromDirectory`
  在 Windows 上都会把 zip 条目名写成反斜杠，不符合 ZIP 规范
  （macOS/Linux 解压会得到一堆名字带 `\` 的散文件）。
  必须用 `ZipArchive.CreateEntry()` 手写正斜杠条目名。
- **`Remove-Item`** 在本环境某些路径下会被沙箱拦截，用 Bash 的 `rm` 更稳。
- **`@{}` 用 `.ContainsKey()`，`[ordered]@{}` 用 `.Contains()`** —— 前者是 Hashtable，
  后者是 OrderedDictionary，方法名不一样，写混了会在运行时才炸。
- **空数组和 `$null` 比不得**：`$null -ne @()` 会走数组比较的分支，不是你以为的布尔。
  想判断「加载过没有」就老老实实另开一个 `$loaded = $false` 标志位。
- **画 PNG 用 `System.Drawing`**（`Add-Type -AssemblyName System.Drawing`），
  PowerShell 5.1 自带。`Image::FromStream($ms)` 之后**那个 MemoryStream 不能关**，
  Image 对象一直靠着它读像素。
- **`Get-FileHash` 要缓存**：一次构建里同一个文件会被问好几遍（占位图判定 + 清单排序），
  不缓存就是几十次重复 IO。
- **`[int]` 是四舍五入，不是截断**：`[int](4/6)` = 1。算网格行号 / 索引一律用
  `[Math]::Floor`，否则元素会错位互相覆盖（做素材联络表时踩过）。
- **`System.Drawing` 存 JPEG 要自己给编码器**：`$bmp.Save($p, $jpegEncoder, $encoderParams)`，
  其中 `EncoderParameter([Encoder]::Quality, [int64]84)`。直接 `Save($p)` 会存成 PNG。
- **手机照片必须先按 EXIF 转正**：`Image.FromFile` 不会自动应用方向。
  读 `PropertyItem(274)`，3/6/8 分别对应 `Rotate180` / `Rotate90` / `Rotate270`，
  转完记得 `RemovePropertyItem(274)`，否则某些看图器会再转一次。
- **用 bash 的 heredoc 往外写 PowerShell 脚本时，反斜杠会被吃掉一层**：
  `-replace '\\','/'` 落到文件里变成 `-replace '\','/'`，PowerShell 报「正则表达式模式无效」。
  写路径规整这类代码时改用 `.Replace([char]92,[char]47)`，绕开引号和正则两重转义。
  （顺带：heredoc 写出来的 `.ps1` 记得**手动补 UTF-8 BOM**，
  `printf '\xEF\xBB\xBF' > x.ps1 && cat body.ps1 >> x.ps1`，否则中文路径读不出来。）

---

## 12. 参考资料

- 用户提供的提示词文档：`D:\下载\新建 Microsoft Word 文档.docx`
  （其中 §18 的优先级链条和「图片必须真实文件、禁止 base64」两条最有价值）
- **Figma 源文件：项目根目录的 `my web figma.svg`**（`D:\下载\` 下还有一份副本）
  顶栏那七张画板叫「主页顶部栏展示」，展开态的所有数值都是从那里量的，
  组名是 HTML 实体编码的中文，用 `grep -bo` 找字节偏移再 `dd` 截片段最省事
- **`网页图片/`（项目根目录，85 张 248MB）** —— 用户提供的原始素材库，
  §3.4 那批封面就是从这里挑的。**站点不引用它，部署时排除掉。**
  重新分配封面时可以先做一张联络表（把每张缩到 200px 拼成网格、角上画序号）
  再挑，比一张张打开快得多；`System.Drawing` 里注意 `[int](4/6)` 会**四舍五入成 1**，
  算行号得用 `[Math]::Floor`，否则缩略图会互相盖掉
- **稿子里内嵌了三张 JPEG**（`data:image/jpeg;base64,`），是占位图机制的图源
  （现在没有实例在用，见 §3.4）：
  `image0` 印度脚铃照片 1460×1536 · `image1` 草莓音乐节照片 1080×1619 ·
  `image2` 草莓酱立绘 1474×1192。手工取出来看的话：
  ```bash
  grep -bo "data:image/jpeg;base64," "my web figma.svg"        # 拿字节偏移
  tail -c +<偏移+24> "my web figma.svg" | head -c 600000 \
    | tr -d '\n' | sed 's/".*//' | base64 -d > out.jpg
  ```
  构建时 `build.ps1` 的 `Get-Seeds` 干的是同一件事（在内存里，不落 base64）
- 用户参考的第三方实现：`D:\下载\index (1).html` / `style.css` / `main.js`
  （其 `.reveal` / `.is-visible` 在 CSS 里根本没定义，入场动画实际未生效；搜索结果也无样式）
- 用户自己部署的旧版：`https://jianghao.kdns.fr`
  （早期星体参照过它：`.gradient-orb` 三段渐变 + `blur(2px)` + 6s `floating` 浮动）
